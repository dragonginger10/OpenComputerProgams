{--This is an example for a programs.cfg file. Please do not add any comments inside actual programs.lua files
  ["fzf"] = {
    files = {
      ["master/fzf/fzf.lua"] = "/bin",--"/" means the file will be placed inside the folder the user specified, defaults to /usr
    },
    --dependencies = {
      --["GML"] = "/lib"--This package is installed into the specified subfolder
    --},
    name = "fzf",--This is for "oppm info"
    description = "fzf written in lua",--This is for "oppm info"
    authors = "dragonginger",--This is for "oppm info"
    -- note = "Additional installation instructions, general instructions and additional information/notes go here, this is an optional line.",
    hidden = false, -- Add this optional line to make your package not visible in "oppm list", useful for custom dependency libraries
    repo="tree/master/lua" --Used by the website. This is where the package will link to on the website
  },
}
